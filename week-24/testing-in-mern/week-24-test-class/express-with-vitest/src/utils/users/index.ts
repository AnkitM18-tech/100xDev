export const userFn = () => {
  return "testUser";
};

// this will be used inside the main application. whatever we have mocked will be used in test file.

// we need to create a __mocks__ folder everytime we want to mock a file, right next to that file level.
